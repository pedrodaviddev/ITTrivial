<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.pedrodavidlp.ittrivial">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <fragment android:name=".login.view.MenuActivity">
        </fragment>
        <fragment android:name=".game.view.fragment.RouletteFragment">
        </fragment>
        <fragment android:name=".login.view.EnterGameActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </fragment>
        <fragment android:name=".game.view.fragment.QuestionFragment">
        </fragment>
        <fragment android:name=".login.view.PlayerListAdminActivity">
        </fragment>
        <fragment android:name=".login.view.PlayerListGuestActivity">
        </fragment>
        <fragment android:name=".login.view.GameListActivity">
        </fragment>
        <fragment android:name=".game.view.fragment.WaitActivity">
        </fragment>

        <activity android:name=".game.view.activity.GameActivity">
        </activity>
    </application>

</manifest>